# Optional: set if API is on another host (leave empty to use Next.js rewrites)
NEXT_PUBLIC_API_BASE_URL=

# Server-side rewrite target (used by next.config.ts rewrites)
# Use 5001 locally — macOS AirPlay occupies port 5000 and returns 403
API_URL=http://localhost:5001
